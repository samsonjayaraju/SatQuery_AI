# Demo samples

Run `python scripts/generate_demo_data.py` to create synthetic, redistributable PNG inputs under `single`, `change` and `cross_modal`. You may replace them with legally obtained benchmark samples; do not commit restricted datasets.
